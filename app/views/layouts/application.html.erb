<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Elmo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <!-- Global Navigation for logged-in users -->
    <% if user_signed_in? %>
      <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-14">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <%= link_to "eLMo", root_path, class: "text-xl font-bold text-indigo-600" %>
              </div>
              <div class="ml-6 flex space-x-4">
                <%= link_to "Dashboard", root_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 text-sm font-medium" %>
                <%= link_to (current_user.admin_role? ? "All Loans" : "My Loans"), loans_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 text-sm font-medium" %>
                <%= link_to "Apply for Loan", new_loan_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 text-sm font-medium" %>
                <% if current_user.admin_role? %>
                  <%= link_to "Admin", admin_root_path, class: "text-purple-600 hover:text-purple-800 px-3 py-2 text-sm font-medium" %>
                <% end %>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-gray-700 text-sm">
                Hello, <%= current_user.full_name || current_user.email.split('@').first %>
              </span>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, 
                  class: "inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500",
                  data: { confirm: "Are you sure you want to sign out?" } %>
            </div>
          </div>
        </div>
      </nav>
      
      <!-- Session Information -->
      <%= render "shared/session_info" %>
    <% end %>

    <!-- Flash messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- Main Content -->
    <div class="<%= user_signed_in? ? 'pt-4' : '' %>">
      <%= yield %>
    </div>
  </body>
</html>
